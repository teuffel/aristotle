Nothing matches
  Falls this won't match, dann Go to a bar 
  Falls nothing happens, dann Do something else Falls nothing happens
  Falls all hell breaks loose, dann Do a third thing

Things not defined
  Falls this won't match, dann Go to a bar 
  Falls this is not found anywhere, dann Do something else 
  Falls all hell breaks loose, dann Do a third thing 

Return on third
  Falls this won't match, dann Return 1 
  Falls this matches, dann Return 3 
  Falls this won't match, dann Return 2

Return on second
  Falls this won't match, dann Return 1 
  Falls this matches, dann Return 2 
  Falls this won't match, dann Return 3 

Return only the first
  Falls this matches, dann Return 1 
  Falls this matches, dann Return 2 
  Falls this won't match, dann Return 3 

Test payload
  Falls this matches, dann Return payload 

Test condition regexp
  Falls this is a regexp condition, dann Return payload 
